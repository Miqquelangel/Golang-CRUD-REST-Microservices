apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "istio-ingressgateway.fullname" . }}-test-connection"
  labels:
    {{- include "istio-ingressgateway.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "istio-ingressgateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
